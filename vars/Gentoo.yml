%YAML 1.1  # expansible.timezone/vars/Gentoo.yml  -*- mode: yaml -*-

# Gentoo timezone variables
---

# command to update /etc/localtime from /etc/timezone file
timezone_file_set: emerge --config timezone-data
